/**
 * Old Operations — Submit a trial lead (Google Sheets backed).
 *
 * Namespaced alias for /api/book-trial. The original path stays live for
 * existing integrations.
 */
export { POST } from '../../book-trial/route';
