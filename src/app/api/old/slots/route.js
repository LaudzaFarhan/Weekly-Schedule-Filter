/**
 * Old Operations — Trial slot availability (Google Sheets backed).
 *
 * Namespaced alias for /api/slots. Requires `Authorization: Bearer <CHATBOT_API_KEY>`,
 * same as the original path, which stays live for existing integrations.
 */
export { GET } from '../../slots/route';
